apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: udagram-frontend
spec:
  selector:
    app: udagram-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
